# rag

> _README to be completed at the end of the project (see `MODULE.md` for the module work notes)._

SecureVault RAG assistant service.

## Setup

```bash
uv sync
uv run uvicorn main:app --reload
```

## Endpoints

- `GET /health` — health check.
